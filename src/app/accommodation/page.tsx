import React from 'react'
import UpcomingRegistration from '../our-meetings/accommodation/[slug]/page'

type Props = {}

const Accommodation = (props: Props) => {
  return (
    <UpcomingRegistration />
  )
}

export default Accommodation